export default function Docs(){return <main className="docs"><a href="/dashboard">← Back to Kernel</a><h1>Kernel Docs</h1><p>Everything you need to publish a static website.</p><div className="doc-card"><h2>1. Create an account</h2><p>Choose <b>Create account</b>, enter your email and an 8+ character password, then you will be taken to your cloud workspace.</p></div><div className="doc-card"><h2>2. Publish a site</h2><p>Open Publish, enter a site name and subdomain, then drag in your website. An <code>index.html</code> file is required.</p></div><div className="doc-card"><h2>3. Your domain</h2><p>Sites use <code>*.example.kernel.app</code>. In production, configure wildcard DNS so every project subdomain reaches the publisher API.</p></div><div className="doc-card"><h2>4. Kernel Assistant</h2><p>Open AI, select <b>Gemini Flash 3.6</b>, enter a prompt, and Kernel opens a VS Code-style code space with an editor and preview.</p></div></main>}
