"use client";
import { FormEvent, useState } from "react";
export default function Login(){const[email,setEmail]=useState("");const[password,setPassword]=useState("");const[error,setError]=useState("");async function submit(e:FormEvent){e.preventDefault();setError("");const api=process.env.NEXT_PUBLIC_API_URL||"http://localhost:4000";try{const r=await fetch(`${api}/auth/login`,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const d=await r.json();if(!r.ok)throw new Error(d.error||"Login failed");localStorage.setItem("token",d.token);window.location.href="/dashboard"}catch(x){setError(x instanceof Error?x.message:"Login failed")}}return <main className="auth"><form className="auth-card" onSubmit={submit}><img src="/kernel-logo.svg" alt="Kernel"/><h1>Log in</h1><p>Access your Kernel Cloud workspace.</p><label>Email<input type="email" value={email} onChange={e=>setEmail(e.target.value)} required autoComplete="email"/></label><label>Password<input type="password" value={password} onChange={e=>setPassword(e.target.value)} required autoComplete="current-password"/></label>{error&&<div className="error">{error}</div>}<button className="publish">Log in →</button><a className="switch" href="/register">Don't have an account? Create one</a></form></main>}
